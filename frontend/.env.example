# Copiez ces variables dans frontend/.env (local)
# ou dans Vercel → Settings → Environment Variables (production)

VITE_SUPABASE_URL=https://votre-projet.supabase.co
VITE_SUPABASE_ANON_KEY=votre_cle_anon_ou_publishable

# Optionnel en local (Vite proxy utilise /api par défaut)
# En production Vercel, /_/backend est utilisé automatiquement
# VITE_API_BASE_URL=/_/backend
